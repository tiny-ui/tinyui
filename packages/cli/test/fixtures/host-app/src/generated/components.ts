// the shape tinyui schema generates for host components: one object per prefix, values are the type names
export const ta = { Icon: "ta.Icon", Loading: "ta.Loading", Badge: "ta.Badge", Unused: "ta.Unused" } as const;
